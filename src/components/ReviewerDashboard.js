import React from 'react';
import { useNavigate } from 'react-router-dom';

function ReviewerDashboard() {
    const navigate = useNavigate();

  return (
    <div>
      <h1>ReviewerDashboard works</h1>
    </div>
  );
}

export default ReviewerDashboard;